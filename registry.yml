server:
  port: 9091
  
eureka:
  instance:
    hostname: registry
  client:
    registerWithEureka: true
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://registry:9091/eureka/
  server:
    wait-time-in-ms-when-sync-empty: 3000
